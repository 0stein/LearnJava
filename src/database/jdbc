jdbc란 자바에서 db를 처리하기 위한 api이다.
jdbc를 이용해서 db에 작업을 하려면 우선 애플리케이션에서 db에 Connection(연결) 되어야 한다.
jdbc에서 db로 연결을 담당하는 인터페이스가 Connection 인터페이스이다.
각각의 데이터베이스 마다 맞는 드라이버를 설치해서 메모리에 올려주는 작업 (Class.forname) 을 해주어야 한다.
